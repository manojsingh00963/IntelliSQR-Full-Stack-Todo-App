export const sendResetEmail = async (to: string, resetLink: string) => {
  // For the assignment we'll log the link. Replace with real email when available.
  console.log(`Send reset link to ${to}: ${resetLink}`);
};
